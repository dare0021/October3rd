#ifndef __CONSTS_H__
#define __CONSTS_H__

#include "cocos2d.h"

static const float TYPE_TIME_MAX = 0.25;
static const float SCALE_SCROLL_INTERVAL = 0.05;
static const float SCALE_MIN = 0.05;
static const float SCALE_MAX = 2;
static const float SCALE_DELTA = 0.2;

#endif // __CONSTS_H__
