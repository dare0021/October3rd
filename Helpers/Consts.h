#ifndef __CONSTS_H__
#define __CONSTS_H__

#include "cocos2d.h"

static const float TYPE_TIME_MAX = 0.25f;
static const float SCALE_SCROLL_INTERVAL = 0.05f;
static const float SCALE_MIN = 0.05f;
static const float SCALE_MAX = 2.0f;
static const float SCALE_DELTA = 0.2f;
static const float ACC_G = 9.80665f;
static const float ZERO_SPEED_THRESHOLD = 1.0f;

#endif // __CONSTS_H__
